<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home | E-Commerce</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-100 to-purple-200 min-h-screen flex flex-col">
  <% var user = typeof user !== 'undefined' ? user : null; %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand font-bold" href="/">E-Commerce</a>
      <div class="d-flex">
        <% if (!user) { %>
          <a href="/login" class="btn btn-light me-2">Login</a>
          <a href="/register" class="btn btn-light me-2">Register</a>
        <% } else { %>
          <a href="/dashboard" class="btn btn-light me-2">Dashboard</a>
          <a href="/logout" class="btn btn-light me-2">Logout</a>
        <% } %>
        <a href="/products" class="btn btn-success">Products</a>
      </div>
    </div>
  </nav>
  <div class="container flex-grow d-flex align-items-center justify-content-center">
    <div class="text-center p-5 bg-white rounded shadow-lg mt-5">
      <h1 class="text-4xl font-bold mb-3">Welcome to E-Commerce!</h1>
      <% if (!user) { %>
        <p class="text-muted mb-4">Your one-stop shop for the latest products. Please login or register to start shopping.</p>
        <a href="/products" class="btn btn-primary btn-lg">Browse Products</a>
      <% } else { %>
        <p class="text-muted mb-4">Welcome back, <%= user.email %>!</p>
        <a href="/dashboard" class="btn btn-primary btn-lg">Go to Dashboard</a>
      <% } %>
    </div>
  </div>
</body>
</html> 